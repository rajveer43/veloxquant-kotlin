/**
 * `veloxquant-java` — `.future()`-based `CompletableFuture` wrappers and purpose-built
 * callback methods (`streamBlocking`, `monitorBlocking`) for Java/Android-Java callers who
 * don't want to touch Kotlin coroutines directly.
 *
 * **Android-compatibility status: Android-safe.** Plain JAR; depends only on
 * `veloxquant-core` and `kotlinx-coroutines-jdk8`. See plan §2.
 */
package io.github.rajveer43.veloxquant.java

/** Placeholder pending Phase 1 (`chatAsync`, `streamBlocking`). */
internal object JavaInteropPlaceholder
