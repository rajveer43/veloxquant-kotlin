/**
 * `veloxquant-runtime` — `veloxquant` CLI process lifecycle (`serve` launch/readiness/shutdown)
 * and one-shot CLI shell-outs (`recommend`, `auto-config`, `methods`, `precompute`,
 * `benchmark`).
 *
 * **Android-compatibility status: JVM-desktop only, never Android-safe.** There is no
 * `ProcessBuilder`-launchable `veloxquant` binary or shell access from an Android app. See
 * plan §2.
 */
package io.github.rajveer43.veloxquant.runtime

/** Placeholder pending Phase 3 (CLI shell-out helpers) and Phase 4 (`VeloxQuantProcess`). */
internal object RuntimePlaceholder
