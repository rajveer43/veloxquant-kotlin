/**
 * `veloxquant-core` — chat/streaming HTTP client, error model, structured output, embeddings,
 * conversations, AutoPilot session surface.
 *
 * **Android-compatibility status: Android-safe.** Plain JAR, no `java.desktop`, no
 * `ProcessBuilder`/shell APIs, compile-time `kotlinx.serialization` codegen only. See plan §2.
 */
package io.github.rajveer43.veloxquant.core

/** Placeholder pending Phase 1 (`VeloxQuantClient`, chat/streaming, error model). */
internal object CorePlaceholder
